{
  "name": "bcp_address_wiki",
  "version": "1.0.0",
  "description": "A address wiki for BCP server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "deploy": "bash deploy.sh"
  },
  "author": "StarTrails",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^1.5.4"
  }
}
